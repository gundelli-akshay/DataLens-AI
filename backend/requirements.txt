# Web Framework
fastapi==0.115.12
starlette<0.47.0
anyio==4.14.2
uvicorn[standard]==0.34.3

# Configuration
pydantic-settings==2.9.1
python-dotenv==1.1.0

# File Uploads
python-multipart==0.0.20

# Data Analysis
pandas==2.2.3
openpyxl==3.1.5

# AI / LLM
groq
google-genai

# Document Extraction & RAG
pymupdf==1.28.2
python-docx==1.2.0
# sentence-transformers: disabled in production (torch ~300 MB exceeds Render free tier 512 MB limit).
# The deterministic CRC32/n-gram fallback in rag.py is used instead.
# Uncomment for local development with sufficient memory:
# sentence-transformers==6.1.0

# Added as the project grows
sqlalchemy==2.0.54
psycopg2-binary>=2.9.9

# Authentication & Security
bcrypt==5.0.0
pyjwt==2.14.0
google-auth==2.58.0
httpx>=0.27.0
